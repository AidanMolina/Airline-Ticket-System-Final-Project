/*import static org.junit.Assert.*;
import org.junit.Test;

public class TicketTestClass {
	
	@Test
	public void testCount() {
		Ticket a = new Ticket(Flight f, LocalDateTime d, Boolean true);
		assertEquals((1000 + (.2*a.thisFlight.getTotalDistance())), a.setPrice());
		assertEquals(1000, a.getPrice());
		assertEquals("First Class flight to " + flight.getArrival() + " from " + flight.getDeparture() " booked on " + dateBooked, ticketInfo);
	}
}

 */

